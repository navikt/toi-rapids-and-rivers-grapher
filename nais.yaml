apiVersion: nais.io/v1
kind: Naisjob
metadata:
  labels:
    team: toi
  name: toi-rapids-and-rivers-grapher
  namespace: toi
spec:
  kafka:
    pool: {{ kafka_pool }}
  image: {{ image }}
  replicas:
    min: 1
    max: 1
  schedule: "0 0 * * *"